<?xml version="1.0"?>
<!--
  CIS Apache Cassandra 4.0 - Logback Configuration
  CIS 4.1 - Ensure logging is enabled
  
  Default level: INFO
-->
<configuration scan="true">
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
    <encoder>
      <pattern>%-5level [%thread] %date{ISO8601} %F:%L - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>/var/log/cassandra/system.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>/var/log/cassandra/system.log.%d{yyyy-MM-dd}.gz</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%-5level [%thread] %date{ISO8601} %F:%L - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Root logger - CIS 4.1 requires INFO level -->
  <root level="INFO">
    <appender-ref ref="STDOUT" />
    <appender-ref ref="FILE" />
  </root>

  <!-- Application loggers -->
  <logger name="org.apache.cassandra" level="INFO"/>
  <logger name="org.cassandra" level="WARN"/>
  
  <!-- CIS Security workload logger - set to WARN -->
  <logger name="org.cisecurity.workbench" level="WARN"/>
  
  <!-- Audit logging -->
  <logger name="org.apache.cassandra.audit" level="INFO"/>
</configuration>
